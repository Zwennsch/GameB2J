still work on the update to do:
    cannot figure out how to integrate the interpolation. lastHeadx is always equal to headX ! Therefore the product (headx - lastHeadx)*interpolation is always 0.
    if I take out the last while loop with the thread.yield() method, the app runs pretty smooth...
    